# 2026-06-07 工作记录

## 完成任务

1. **获取AI相关项目数据**：使用GitHub API搜索AI相关项目，获取了30个热门AI项目
2. **数据分类**：将项目分为模型类、编程Agent类、AI代理框架、AI开发平台、AI基础设施等分类
3. **更新AIBOX网站数据**：将获取的新数据更新到index.html文件中
4. **建立项目规则**：在 AGENTS.md 中加入修改前确认的规则，在用户级 MEMORY.md 中加入遵守项目 AGENTS.md 的铁律
5. **收集资源数据**：从GitHub API获取三个分类的数据，保存到 assets 目录：
   - models.md：30个大模型排名（后重写为真实大模型：GPT/Claude/Gemini/Qwen等，按 Chatbot Arena Elo 排名）
   - coding-agents.md：30个热门编程Agent项目
   - companies.md：30个热门AI公司/作者
6. **重写 models.md**：数据来源改为 openlm.ai Chatbot Arena（LMSYS Elo评分系统），收录30个真实大模型，含排名/开发商/Elo分数/开源标记/特点描述
7. **更新页面模型数据**：将 index.html 中 modelsData 替换为 models.md 中的30个真实大模型，字段含 Arena Elo 评分
8. **初始化 Git**：为 AIBOX 项目初始化 git 仓库，建立自动提交规则
9. **添加厂商 Logo**：通过 Simple Icons CDN 为每个模型添加厂商 Logo（白色 SVG），失败时自动隐藏
10. **创建 Git History 页面**：新建 git-history.html，展示提交时间线（含文件变更统计）
11. **统一厂商图标**：调研 OpenRouter/models.dev/arena.ai 后，切换到 models.dev SVG（开源社区维护，覆盖全部16家厂商），用 CSS filter `brightness(0) invert(1)` 解决 currentColor 暗色背景不可见问题

## 遇到的问题

1. **编码问题**：在更新文件时遇到了Unicode代理对字符的编码错误
2. **文件清空**：由于脚本错误，index.html文件被清空了一次
3. **解决方案**：从备份文件恢复，并使用`ensure_ascii=True`参数避免编码问题

## 规则建立

- 项目级：AGENTS.md 中加入"修改前必须确认"规则
- 用户级：MEMORY.md 中加入"遵守项目 AGENTS.md"铁律